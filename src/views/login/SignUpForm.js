import React from "react";

const SignUpForm = ({ loading, user, handleLoginClick }) => (
  <div className="text-center mt-4">
    {/* Aquí irían los campos de registro si los hubiera */}
  </div>
);

export default SignUpForm;
